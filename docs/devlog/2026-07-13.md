# 2026-07-13 Devlog

## 11:16 EST - M5 Unified Editor: spec + interactive prototype (Phase 0)

### Summary
Gaylon returned wanting the clunky macOS edit/checkoff UX overhauled to a Things 3 standard. Full plan-mode cycle produced a decision-complete implementation spec (M5) and a working interactive HTML prototype of the new interaction model for both platforms — the prototype must be approved before any Swift is written.

### What Was Done
- **Root-cause exploration** (two parallel explore passes over the editor and check-mode/consistency surfaces). Five of Gaylon's seven reported macOS bugs reduce to two mechanisms:
  - The M4 `.onMove` drag recognizer is armed on every row at rest → ~0.5s click-to-highlight latency and flaky double-click rename. This is exactly the V1-4/D10 gate the M4 spec predicted; the pre-authored hover-gating fix was never applied.
  - Fragile `.onAppear { focused = true }` focus grabs → keystrokes fall through to the sidebar's `.searchable` (the "typing triggers search" mystery). Plus: `focusedCommandActions` closures capture `selectedID` by value at publish time → ⌘[/⌘] act on the last-added row.
  - Also confirmed: check mode has **zero** keyboard support (no selection model at all — the unfixed half of the 07-07 testing note), multi-select drag is a deliberate-but-silent no-op, the ellipsis menu hides shortcut hints because its target row may not equal the selection.
- **User decisions locked**: unify edit/check modes into ONE view (checkbox always visible, All/Remaining/Completed filter replaces the mode switch); Things 3 is the feel benchmark; **Return = rename** (Things-style). Consequence cascade: ⌘N = New Item (Things parity), New List → ⇧⌘N, ⌥⌘N = Add Above (bare ⇧Return dropped), ⌘E = menu-visible rename twin, ⌘K = Toggle Checked, Space = toggle check, Esc also clears selection, ⇧⌘E retired.
- **Spec written**: `docs/2026-07-13-m5-unified-editor-spec.md` (Rev 2) — complete interaction contract, all B1–B7 fixes, `CommandCatalog` registry-lite (menus + help doc render from one source; help is *generated*, so it cannot drift), deletion list (CheckModeView/CheckRowView/isCheckMode die), 4 phases each with a manual acceptance gate, file-by-file map, test migration map, ranked risks.
- **Interactive prototype built and published** (Claude artifact `e607fd31…`): a working simulation, not static mockups — macOS window (menu bar, toolbar, outline, inspector, context menus, full keyboard map, hover-armed drag, confirmation dialogs) and iOS iPhone frame (row-density check, tap/long-press/add-flow). Right rail: live keyboard legend + gate-matching "try this" script.
- Planner-agent workflow note: the design agent initially overrode the Return decision with the outliner convention; sent back for revision — Rev 2 delta honors it and resolved all downstream collisions (verified Return vs dialogs/fields/List defaults).

### Files Modified
- `docs/2026-07-13-m5-unified-editor-spec.md` (new — the M5 contract)
- Prototype HTML in session scratchpad, published as artifact (not committed; it's a design tool, not product code)
- Plan file `~/.claude/plans/warm-doodling-wind.md` (session-side)

### Key Technical Insight
The M4 drag work shipped with its own predicted regression unverified: `.onMove` arms a drag recognizer on every row and delays click recognition on macOS — the spec's D10 hover-gating contingency existed but was never applied because the V1-4 manual gate was skipped. Lesson: a specced contingency with an unverified gate is a live bug, not a hedge. Also: SwiftUI `focusedSceneValue` closures must read state live at invocation — by-value captures of selection are stale by design, since republication lags.

### Next Steps
- Gaylon reviews the prototype (especially the Remaining-filter Space-Space-Space flow and iOS row density); prototype + spec revised together if anything feels wrong.
- On prototype approval (Phase 0 gate): commit the spec, open td epic + phase tasks, start the implementer on Phase 1 (foundations: hover-gated drag, focus architecture, live-read commands, multi-drag refusal) — Phase 1 alone makes today's app feel right before any unification.
- Phases 2–4 follow their gates: unification → keyboard completion → CommandCatalog + generated help. CODEX2 hostile review per phase commit batch; TestFlight dogfood after Phase 4.

## 22:35 EST - M5 Phase 1 implemented, committed (c03d001, 2abf37a)

### Summary
Phase 0 gate passed (prototype approved after two testing rounds). Implementer executed Phase 1 with two textbook stop-and-reports; both resolved at spec level (Rev 2.2) with evidence, then all-green: 193/193 unit, 8/8 macOS UI, 5/5 iOS UI.

### What Was Done
- B1/B4a hover-gated drag arming — with a spec correction: shared-ancestor hover @State kills the in-flight AppKit drag session (List re-diff); row-local state validated and adopted (Rev 2.2).
- B2/B4b EditorFocus architecture — one @FocusState owner, task-deferred from store state; both .onAppear focus grabs deleted.
- B3 live-read command closures + regression test (testCommandBracketIndentsSelectedRow — ⌘] hits the middle row, not the last).
- B6 multi-selection rows never lift a drag.
- BONUS root-cause: pre-existing indent() bug — new parent never expanded, so an indented row VANISHED (probably a chunk of the "⌘] is unpredictable" complaint since V1). Fixed + unit test.

### Key Technical Insight
SwiftUI macOS List + .onMove: any ancestor @State change during a native drag re-diffs the ForEach and invalidates the drag session. Gate per-row behavior with ROW-LOCAL state. Also: presentation state (expandedIDs) must be maintained by every reparenting mutation, not just addChild.

### Next Steps
- Gate M1 (Gaylon, ~2 min): instant clicks ×10, add-flow Return chain with zero search leaks ×10, double-click rename ×10, ⌘]/⌘[/⌘⌥↑↓ on a middle row, drag reorders, multi-select drag refuses.
- CODEX2 hostile review of c03d001+2abf37a on Gaylon's go.
- Then Phase 2 (unification — the single screen).
